emp = { id: 1, name: "xxxx", mobile: 1123 };
console.log(
  " ID " + emp.id + " Name : " + emp.name + " Mobile : " + emp.mobile
);
jsonob = JSON.stringify(emp);
ob = JSON.parse(jsonob);
console.log("OB: " + ob.name + " " + ob.id + " " + ob.mobile);
console.log("JSON " + jsonob);
var x = document.getElementById("demo");
function getLocation({
    if(navigator.geolocation){
        
    }
})