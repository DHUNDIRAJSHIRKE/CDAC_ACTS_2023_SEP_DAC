DELIMITER &&
CREATE PROCEDURE emp_sal_avg()
BEGIN
	SELECT AVG(SALARY) FROM worker;
END&&
DELIMITER;


DELIMITER && 
CREATE PROCEDURE get_emp_from_id(id int)
BEGIN
	SELECT * FROM worker WHERE WORKER_ID=id;
END&&
DELIMITER;